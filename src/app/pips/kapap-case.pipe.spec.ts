import { KapapCasePipe } from './kapap-case.pipe';

describe('KapapCasePipe', () => {
  it('create an instance', () => {
    const pipe = new KapapCasePipe();
    expect(pipe).toBeTruthy();
  });
});
